# TODO — что нужно от тебя перед выкладкой

Сайт собран и собирается (`npm run build`). Ниже то, что я не мог заполнить сам:
выдумывать эти данные нельзя, а плейсхолдеры оставлены ровно там, где спека их
и просила.

---

## 1. Плейсхолдеры — обязательно

Всё лежит в одном файле: [`src/data/site.ts`](src/data/site.ts).
Больше нигде эти значения не дублируются.

| Плейсхолдер | Где отображается | Что подставить |
|---|---|---|
| `{ИМЯ}` | шапка, H2 светлой секции, подвал, `<title>`, JSON-LD | имя и фамилия |
| `{ТЕЛЕФОН}` | шапка (моб.), форма, подвал | номер в читаемом виде |
| `phoneHref` | `href="tel:"` | тот же номер цифрами: `tel:+79178568489` |
| `{TG}` | кнопки Telegram | полная ссылка `https://t.me/...` |
| `{WA}` | кнопки WhatsApp | `https://wa.me/7917...` |
| `{КАНАЛ}` | пилюля-анонс, кнопка светлой секции | юзернейм канала без `@` |
| `{ИНН}` | нижняя строка подвала, оферта | ИНН или ОГРНИП |

Отдельно: `SITE.url` сейчас `https://example.ru` — заменить на боевой домен,
иначе canonical, OG и sitemap будут указывать не туда.

---

## 2. Решения, которые за тебя принимать не стал

**Метрика.** `SITE.metrikaId` = `null`. Пока так, вызовы целей молча не срабатывают.
Поставь номер счётчика — и заработают цели `lead_sent`, `tg_click`, `open_site`,
`tg_post`, `hero_tab`. Сам скрипт счётчика надо будет вставить в
[`src/app/layout.tsx`](src/app/layout.tsx) (я не стал: без номера вставлять нечего).

**Пилюля-анонс над H1** сейчас говорит «Новый разбор: лендинг для автосервиса».
Текст из спеки, но разбора такого у тебя может не быть — поправь на реальный
свежий пост в [`src/components/sections/Hero.tsx`](src/components/sections/Hero.tsx).

**Telegram-бот для заявок.** В [`public/api/lead.php`](public/api/lead.php) две константы:
`BOT_TOKEN` и `CHAT_ID`. Токен получить у `@BotFather`, свой chat id — у `@userinfobot`.
Токен живёт только в этом файле на сервере, в бандл фронтенда не попадает.
После заливки проверить форму с чужого телефона — это пункт чеклиста.

**Кейсы работ.** У всех проектов `caseUrl: null`, поэтому основной кнопкой в
витрине стоит «Открыть сайт». Появятся нишевые страницы или разборы — проставь
`caseUrl`, и кнопка сама станет «Разбор проекта», а «Открыть сайт» уедет в
мелкую текстовую ссылку. Код при этом не трогается.

**Посты в канале.** У всех `tgPost: null` — кнопка «Пост в Telegram» не рендерится
вообще (не серая, не «скоро»). Появятся посты — проставь ссылки.

**Нишевые страницы.** В [`src/data/niches.ts`](src/data/niches.ts) у всех ниш
`href: null`, поэтому бегущая строка и колонка в подвале — текст, а не битые ссылки.
Появятся страницы — проставь `href`, ссылки включатся сами.

---

## 3. Что проверить глазами

- [ ] `Massage Niznek` — единственная работа с `isDemo: false`. Остальные девять
      помечены «демо-концепт». Если какие-то из них на самом деле клиентские —
      скажи, поправлю в [`src/data/projects.ts`](src/data/projects.ts).
- [ ] `Старый Амбар` — я пометил его демо-концептом. Это реальный ресторан;
      если сайт делался по заказу, флаг надо снять.
- [ ] Описания работ в `projects.ts` я написал по содержимому самих сайтов.
      Прочитай — там может быть неточность про задачу, которую знаешь только ты.

---

## 4. Найдено попутно, не связано с этим сайтом

Замерил Lighthouse по твоим работам (мобильный, 10.08.2026):

| Работа | Performance | LCP |
|---|---|---|
| clinic-template | 97 | 2,2 с |
| sports-nutrition | 96 | 1,4 с |
| massage-niznek.ru | 82 | 3,3 с |
| stomatologya_template | **73** | **15,5 с** |

У стоматологии LCP 15,5 секунды — это не «медленновато», это ломает первый экран.
На сайте я показываю цифру clinic-template (97), она честная и проверяемая,
но стоматологию стоит починить: она в портфолио и её открывают.

---

## 5. Деплой

```bash
npm run build
```

Всё, что нужно выложить, лежит в `out/`. Внутри уже есть `.htaccess`
(gzip/brotli, кэш, 404) и `api/lead.php`. Заливать содержимое `out/` в корень
хостинга целиком.

После заливки проверить, что открывается `/policy/` и `/oferta/` — это проверка
`trailingSlash`, без него внутренние страницы на Apache отдают 404.

---

## 6. Ассеты

Отдельным списком в [`assets-todo.md`](assets-todo.md).

---

## 7. Чеклист части 8 — что прошло, что нет

Прошло, проверено:

- ноль эмодзи в собранном HTML; иконки только Lucide, обводка везде 1.5
- ноль абстрактных 3D-фигур и блобов — внутри панелей только реальные экраны
- не больше двух свечений на экран: первый экран, витрина, форма — по одному
- ровно три эффектных компонента: Spotlight, Glowing Effect, BorderBeam
- шрифтов ровно три, каждый в своей роли
- акцентное слово везде одним способом — курсивный Playfair
- ни одной выдуманной цифры
- цена видна на первом экране и в прайсе
- телефон текстом в `<a href="tel:">`
- один `<h1>`, 10 `<h2>`, 26 `<h3>`
- **JS отключён — страница читается целиком**, все ссылки на месте, первый
  вопрос FAQ раскрыт, карусель вырождается в ряд превью
- карусель и живое окно не перехватывают скролл страницы
- инпуты 16px, тапы 44×44
- `prefers-reduced-motion` проверен эмуляцией: карусель становится рядом,
  бегущая строка стоит, луч в тарифе выключен
- внутренние страницы открываются после экспорта — `/policy/` и `/oferta/`
  отдают 200 из статики
- внешних ссылок без `rel="noopener"` — ноль
- у всех 16 картинок есть `alt` и `width`/`height`

Проверить нужно тебе:

- [ ] **микроразметка в валидаторе Яндекс.Вебмастера** — прогнать после того,
      как появится боевой домен: сейчас в JSON-LD стоит `example.ru`
- [ ] **форма реально доставляет заявку** — проверить с чужого телефона после
      заливки, когда будут заполнены `BOT_TOKEN` и `CHAT_ID`
- [ ] **тест на силуэт** — уменьшить скриншот главной до 200px и положить
      рядом с конкурентами из выдачи

Про одно исключение: заголовок FAQ «Почему сайт стоит 45 тысяч, а не 15?»
формально начинается с «Почему», но это вопрос клиента из спеки, а не
«Почему выбирают нас». Запрет был про второе, оставил как есть.
