import { SectionHead, W } from '@/components/SectionHead'
import { Reveal } from '@/components/Reveal'

/**
 * Как идёт работа. Пять шагов вместо обещания «сделаем красиво».
 *
 * Ни одного нового факта здесь нет: предоплата 50%, структура до дизайна,
 * срок 7–10 дней, правки в тот же день и 30 дней после запуска,
 * домен и исходники на заказчика — всё это уже сказано в герое, в прайсе,
 * в сравнении и в вопросах. Раньше эти обещания лежали по разным секциям
 * поодиночке; здесь они выстроены в порядок, в котором человек их проживёт.
 *
 * Это тот самый блок, которого на странице не хватало для чека в 45–120 тысяч:
 * до него страница отвечала «что» и «почём», но не отвечала «как это будет
 * происходить со мной» — а именно этот вопрос человек задаёт перед оплатой.
 */
const STEPS: { n: string; title: string; body: string }[] = [
  {
    n: '01',
    title: 'Бриф в переписке',
    body: 'Без созвона на час и без анкеты на сорок пунктов. Спрашиваю про бизнес, клиентов и то, что у них вызывает сомнения. Отсюда вырастут структура и тексты.',
  },
  {
    n: '02',
    title: 'Предоплата 50% и прототип',
    body: 'Прототип — это структура будущей страницы: что за чем идёт и почему. Мы согласуем её до дизайна, поэтому вы видите, что получится, ещё до первой отрисованной кнопки.',
  },
  {
    n: '03',
    title: 'Дизайн и тексты',
    body: 'Тексты пишу сам, по брифу. Правки обсуждаются в переписке и делаются в тот же день — согласование не растягивается на недели.',
  },
  {
    n: '04',
    title: 'Запуск',
    body: 'Лендинг — 7–10 дней от брифа. Домен, хостинг и исходники оформляются на вас, остаток оплаты — после сдачи.',
  },
  {
    n: '05',
    title: 'Месяц правок',
    body: '30 дней после запуска правки бесплатно. Уйти к другому разработчику можно в любой момент — всё уже ваше.',
  },
]

export function Process() {
  return (
    <section id="kak-rabotayu" className="section pt-0" aria-labelledby="kak-h">
      <div className="container">
        <SectionHead eyebrow="как идёт работа" id="kak-h">
          Пять шагов. Структура — <W>до</W> дизайна.
        </SectionHead>

        <ol className="mt-12 flex flex-col gap-3">
          {STEPS.map((s, i) => (
            <Reveal key={s.n} delay={Math.min(i, 4) * 50}>
              <li className="glass-flat flex gap-4 rounded-[var(--r-md)] p-5 sm:gap-6 sm:p-6">
                <span className="t-micro shrink-0 pt-1 tabular-nums">{s.n}</span>
                <div className="min-w-0">
                  <h3 className="t-h3 text-[1.0625rem] sm:text-[1.1875rem]">{s.title}</h3>
                  <p className="t-body mt-2 text-[0.9375rem]">{s.body}</p>
                </div>
              </li>
            </Reveal>
          ))}
        </ol>
      </div>
    </section>
  )
}
